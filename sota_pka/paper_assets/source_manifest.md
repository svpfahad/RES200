# Source Manifest

This folder will contain generated benchmark tables, plots, QLattice formulas,
and source/citation manifests. Initial public-source candidates are listed in
`configs/public_sources.csv`.

Claims must remain tied to regenerated metrics from `runs/**/predictions_*.csv`.
