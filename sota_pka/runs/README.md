# Runs

Each run folder should contain:

- `metrics.csv`
- `predictions_<model>.csv`
- fitted model artifacts when practical
- environment/package freeze
- config JSON/YAML used for the run
- notes on skipped dependencies or failed integrations
